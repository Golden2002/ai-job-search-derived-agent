# 《基线能力清单》— ai-job-search-derived-agent（通用简历制作工具主基线）

> **基线位置**：`D:\项目\04_通用简历\baseline_main\`
> **基线身份**：fork 自 MadsLorentzen/ai-job-search 的通用求职 Agent 框架，改造升级为「通用简历制作 / 职业发展」独立产品（PAEG 工具生态）
> **双形态**：① 根目录 = Claude Code 求职工作流框架（12 个命令 + 9 个技能 + 6 个门户 CLI + 8 个工程工具）；② `product/` = 网页独立产品 + MCP 标准化插件
> **拆解方法**：逐文件通读 README/REQ/AGENTS/SETUP/CHANGELOG、`.claude/commands/` 全部 12 个命令、`.claude/skills/` 全部 9 个技能、`.agents/skills/` 全部 6 个门户 CLI、`tools/` 全部 8 个工具、`product/src/resume_product/` 全部 27 个 Python 模块、`product/web/index.html`、全部测试文件。以下每项能力均真实存在、指向具体文件，验证方式给出测试文件或命令。

---

## 一、能力拆解总表（按功能域分层，编号即能力 ID）

### A. 产品化核心引擎（`product/src/resume_product/` + `product/web/`）

| 编号 | 能力名称 | 能力描述 | 模块/文件位置 | 验证方式（测试/命令） |
|---|---|---|---|---|
| A01 | 经历采集（LLM 事实卡） | 原始经历文本 → 结构化事实卡（claim/evidence/quote），LLM 提示词含口语规范化要求（口语/自嘲→书面简历语言、保留量化数据、绝不编造），LLM 失败自动降级确定性按句拆解 | `product/src/resume_product/core.py`（`ResumeEngine.enrich` / `_heuristic_extract`） | `product/tests/test_core.py::test_enrich_experience_quote_gate`、`test_llm_lang.py::test_enrich_llm_normalizes_colloquial` |
| A02 | 事实校验门（ClaimGate） | 主张三档校验：verified（有证据）/ unverified（缺证据，不得静默写入）/ exaggerated（含强度词但证据弱）；未确认信息不升级、绝不编造；ConfirmationGate 生成待确认问题清单 | `product/src/resume_product/claim_gate.py` | `product/tests/test_batchD.py::test_claim_verified/test_claim_unverified_no_evidence/test_claim_exaggerated` |
| A03 | 定向表达（Role Pack 适配） | 事实卡 → 定向简历，按 Role Pack 的 priorities/value_mappings 调整表达重点；输出前接入 PAEG 语言规范模块 `paeg_lang_style.fix_known_gaffes`（病句/口语修正，可注入 chat_fn） | `product/src/resume_product/core.py`（`ResumeEngine.compose`） | `product/tests/test_llm_lang.py::test_compose_applies_lang_fix` |
| A04 | 简历多格式导出 | 结构化事实卡 → Markdown / HTML / Word（python-docx 专业排版，缺依赖降级纯文本）/ PDF；`generate_resume(experiences, target_role, format)` 便捷 API | `product/src/resume_product/core.py`（`to_html`/`to_docx`/`to_pdf`/`generate_resume`） | `product/tests/test_core.py::test_generate_resume_html`、`test_docx.py`、`test_pdf.py` |
| A05 | PDF 固定资产渲染（多模板） | Markdown → HTML（结构类名契约 .r-title/.r-adapt/.r-exp/.r-claim/.r-evidence）→ Playwright + Chrome A4 PDF（页脚页码）；CSS 加载优先级：custom_css（用户最高）→ templates/{template}.css → resume.css 兜底；**3 风格模板：classic 经典素雅（宋体黑白）/ modern 现代风格（教育蓝左色带卡片感）/ minimal 极简单栏**；Chrome 路径环境变量/多平台自动探测 | `product/src/resume_product/render/render_pdf.py` + `render/resume.css` + `render/templates/{classic,modern,minimal}.css` | `product/tests/test_render_templates.py`（7 用例：三模板元数据、CSS 加载、自定义覆盖、结构契约、XSS 转义） |
| A06 | LaTeX 编译链 + 视觉校验循环 | lualatex 编译（nonstopmode、120s 超时、清晰错误）；视觉校验：页数硬要求（CV=2 页）、孤儿标题检测（大写标题后无内容）、字体一致性（替换字符探测）；对应根基线「编译-检查-迭代」闭环 | `product/src/resume_product/render/latex_compile.py`（`compile_tex`/`visual_check`） | `product/tests/test_batchC.py::test_compile_no_lualatex_clear_error/test_visual_check_two_pages_pass/test_visual_check_orphan_heading`；根基线 `tools/verify_pdf.py` |
| A07 | **职位五维度匹配评分** | EligibilityGate（公民/永居/安全许可硬门 FAIL+原文引用，欢迎国际申请者 PASS，沉默 PROCEED_UNVERIFIED）+ LanguageGate（未声明语言 FAIL 硬停、声明级别低于要求 FLAG 不自动拒、其余 PASS）+ 五维评分（technical 技术 30%、experience 经验 30%——按职能而非头衔、behavioral 行为 15%、location 地点 PASS/FAIL/FLAG 定性、alignment 职业对齐 25%），输出 total/verdict(Strong/Moderate/Weak/Poor Fit)/gaps | `product/src/resume_product/job_evaluation.py`（`evaluate_fit`/`EligibilityGate`/`LanguageGate`） | `product/tests/test_batchA.py`（15 用例：资格门/语言门/低分/高分全覆盖） |
| A08 | **相关性加权裁剪** | 简历溢出时按三因子加权裁剪而非机械删旧：score = 0.5×职位相关性 + 0.3×独特性（技能他处少见度）+ 0.2×依赖度（被其他条目引用）；返回保留/移除清单与理由 | `product/src/resume_product/cv_trim.py`（`trim_experience`） | `product/tests/test_batchA.py::test_trim_prioritizes_relevance_over_time/test_trim_returns_reason_for_removed` |
| A09 | 多职位批量评分排序 | 对多个职位逐一跑五维评分，按 total 降序排序，未评分（门 FAIL）排最后，输出 rank 位次 | `product/src/resume_product/rank.py`（`rank_postings`） | `product/tests/test_batchA.py::test_rank_sorts_by_total_desc` |
| A10 | 薪资基准对比 | 输入职位/地区/年限 → p25/p50/p75 分位区间 + 对比结论（低于/符合/高于市场），sources 可扩展外部数据源，无数据时明确标注数据不足 | `product/src/resume_product/salary_benchmark.py`（`benchmark`） | `product/tests/test_batchA.py::test_benchmark_with_sources/test_benchmark_verdict_below_market/test_benchmark_no_sources_insufficient` |
| A11 | 面试准备（STAR） | 生成面试准备包：STAR 四要素故事（S/T/A/R，缺项标注待补充）、高频难题映射（含"缺少 X 技能"桥接策略）、反问问题清单；`simulate_interview` 模拟面试问答结构 | `product/src/resume_product/interview_prep.py`（`prepare`/`simulate_interview`） | `product/tests/test_batchB.py::test_prepare_star_four_elements/test_simulate_interview_structure` |
| A12 | 申请结果跟踪 + 跟进信 | 本地 JSON 申请跟踪器：状态机 applied→interview→offer→rejected→withdrawn、事件时间线、按状态查询；跟进信按 7/14/21 天分档（礼貌确认/适度重申/最后跟进），绝不代发 | `product/src/resume_product/application_tracker.py`（`ApplicationTracker`/`generate_followup`） | `product/tests/test_batchB.py::test_tracker_add_and_list/test_tracker_status_machine/test_followup_tiers` |
| A13 | 技能缺口分析 + 学习路径 | 多职位技能频率热图：权重 (100-fit)/100、记录缺口优先于推断（provenance 记录）、空白 fit 不当 0（跳过并计数）、按权重降序生成学习计划 | `product/src/resume_product/skill_gap.py`（`analyze_gaps`） | `product/tests/test_batchB.py::test_skill_gap_heatmap_weight/test_skill_gap_recorded_beats_inferred/test_skill_gap_blank_fit_skipped` |
| A14 | Profile 三维度 | candidate（教育/经历/技能/发表/奖项）+ behavioral（行为评估/优势/理想环境）+ writing_style（语气/禁忌清单——无 em-dash/无陈词滥调）；JSON 序列化与字段校验 | `product/src/resume_product/profile_3d.py`（`Profile3D`） | `product/tests/test_batchB.py::test_profile_validate_ok/test_profile_validate_missing_skills/test_profile_json_roundtrip` |
| A15 | 能力分类体系 + 经历标签化 | 8 类通用能力维度（communication/leadership/analysis/technical/project_management/domain_expertise/execution/creativity，医学→全行业通用化），按关键词为经历打维度标签 | `product/src/resume_product/capability_taxonomy.py` + `product/data/capability_tags/general_v1.json` | `product/tests/test_batchD.py::test_tag_experience` |
| A16 | 经历拆解六要素 schema | 原始经历 → {research_subject/method/tools/role/deliverable/transferable_skill} 六要素 + target_direction 目标方向表达重点；JSON Schema 校验；LLM 注入或规则模板兜底 | `product/src/resume_product/schemas/canonical_experience.py`（`decompose`/`validate_canonical`） | `product/tests/test_batchD.py::test_canonical_schema_requires_six_fields/test_decompose_fallback_six_fields` |
| A17 | 多版本输出 | 同一经历生成三版横向对比：稳妥版（弱化强度词）/ 专业版（行业术语定向）/ 高竞争力版（量化成果导向）；LLM 可注入，规则模板兜底 | `product/src/resume_product/multi_version.py`（`generate_versions`） | `product/tests/test_batchD.py::test_multi_version_three_versions/test_multi_version_safe_weakens_strong_terms` |
| A18 | 多模板注册/切换/导入 | TemplateRegistry：注册（tex\|html\|md 格式校验 + 文件存在校验）、切换当前模板、导入自定义模板；内置 moderncv_banking（指向根 `cv/main_example.tex`）/resume_html/generic_md | `product/src/resume_product/template_registry.py` | `product/tests/test_batchC.py::test_registry_builtins/test_registry_switch_and_get/test_registry_invalid_format_rejected/test_registry_import_valid` |
| A19 | 职位门户可扩展架构 | Posting 统一契约（title/company/source/url/description/deadline——deadline 缺省 None 绝不猜值）+ JobPortalAdapter 抽象基类（search/detail）+ PortalRegistry 注册表 + JSONFilePortal 示例适配器；真实门户按同接口扩展 | `product/src/resume_product/job_portal.py` | `product/tests/test_batchE.py`（7 用例：deadline 不猜值、search/detail、注册表） |
| A20 | **drafter + reviewer 双 Agent 闭环** | drafter 生成定向简历初稿 → reviewer 确定性审核（ATS 校验 + 标题/长度/量化检查 + 评分）→ 按问题迭代补强（最多 N 轮）→ 输出最终简历 + 审核结果 + 轮数；chat_fn 可注入 | `product/src/resume_product/drafter_reviewer.py`（`draft_resume`/`review_resume`/`drafter_reviewer`） | `product/tests/test_phase5.py::test_draft_resume/test_review_resume/test_drafter_reviewer_flow` |
| A21 | **ATS 文本层校验** | 8 条确定性 ATS 规则（标准标题/无表格/无图片/标准字体/关键词/纯文本可读/联系方式/量化成果），逐项 passed/issues + 0-100 通过率评分，确定性可复现 | `product/src/resume_product/ats_check.py`（`ats_check`） | `product/tests/test_phase5.py::test_ats_rules_defined/test_ats_check_clean_resume/test_ats_check_empty` |
| A22 | **MCP 标准化插件（PAEG 生态）** | stdio MCP server（FastMCP）：3 基础工具（generate_resume/enrich_experience/list_role_packs）+ 基线对齐 17 工具共 **20 工具**（evaluate_fit/trim_experience/rank_postings/salary_benchmark/prepare_interview/simulate_interview/track_application/generate_followup/analyze_skill_gaps/claim_review/generate_versions/decompose_experience/tag_experience/list_templates/register_template/compile_latex/portal_search）+ list_tools 动态发现；统一执行入口 `execute(name, args)` JSON 契约绝不抛异常 | `product/src/resume_product/mcp_server.py` + `tools/schema.py`（20 个 JSON Schema）+ `executor.py`（20 个 handler） | `product/tests/test_mcp.py`（schemas 存在/结构/调用契约）；启动命令 `resume-mcp` |
| A23 | Flask API + 网页全链路 | **13 个 API 端点**：health / role-packs / templates / enrich（含 claim 分档 + 确认问题）/ claim-check / match（五维）/ versions（三版本）/ ats / trim / interview / skill-gaps / generate / export（docx/pdf 文件下载，PDF 支持模板选择 + 自定义 CSS）；前端六步流程（填写经历与 JD → 事实校验 → JD 匹配评分 → 三版本生成 → ATS 校验 → 在线编辑与导出 HTML/Word/PDF） | `product/src/resume_product/api.py` + `product/web/index.html` | `product/tests/test_api.py`（7 用例）；启动 `python -c "from resume_product.api import create_app; create_app().run(port=5123)"` |
| A24 | LLM 客户端 | DeepSeek OpenAI 兼容客户端：key 读取 auth.json → 环境变量 DEEPSEEK_API_KEY 降级，失败返回空串（调用方降级确定性模式），无第三方依赖（urllib） | `product/src/resume_product/llm_client.py` | `product/tests/test_llm_lang.py::test_default_engine_uses_llm`（monkeypatch） |
| A25 | Role Pack 数据资产 | 4 个行业角色包：tech_v1（科技/互联网）、consulting_v1（咨询/专业服务）、finance_v1（金融）、education_v1（教育）——结构含 priorities/value_mappings/preferred_actions/allowed_verbs/restricted_verbs/forbidden_claims/required_evidence/sentence_patterns | `product/data/role_packs/{tech,consulting,finance,education}_v1.json` | `product/tests/test_phase5.py::test_multi_role_packs/test_consulting_role_pack_direction`；`list_role_packs()` |

### B. 主基线命令工作流（`.claude/commands/` 12 个 + 技能命令 2 个）

| 编号 | 能力名称 | 能力描述 | 模块/文件位置 | 验证方式（测试/命令） |
|---|---|---|---|---|
| B01 | /setup 三路径入职 | 路径 A（documents/ 文件夹阅读+交叉引用+幂等合并）、路径 B（单 CV 导入）、路径 C（访谈模式 9 节）；公共 fork 隐私预检（写前检查 origin 可见性）；`--section skills/experience/search` 增量更新；搜索配置含功能优先于职位头衔、潜在职业方向建议 | `.claude/commands/setup.md` | `tests/test_onboarding_privacy.py`（预检先于写文件） |
| B02 | **/apply drafter-reviewer 完整流程** | 8 步：Step0 解析（URL/文本、403 升级、posting 为不可信数据）/ Step1 评估（五维 + 薪资基准）/ Step2 起草（CV+求职信、需求全覆盖、三源事实接地审计）/ Step3 **spawn reviewer agent**（研究公司 + 事实审计 + PartA 结构化编辑/PartB 叙事建议）/ Step4 修订 / Step5 **编译-检查 PDF 循环（lualatev/xelatex、2 页/1 页硬要求、孤儿标题、字体）+ ATS 文本层校验（pdftotext -layout -enc UTF-8、关键词覆盖表、诚实缺口）** / Step6 验证清单 + **Step6b 记录 tracker（drafted 行、14 列头、deadline 迁移）+ 归档 job_posting.md** | `.claude/commands/apply.md` | `tests/test_apply_records_application.py`（15 用例：drafted 行/头匹配/deadline 迁移/归档/子文件夹命名）；`tests/test_latex_guidance.py`；`tests/test_verify_pdf.py` |
| B03 | /scrape 门户搜索编排 | 自动发现 `.agents/skills/*/SKILL.md`、尊重 enabled 开关、CLI 并行搜索（14 天过滤）+ WebSearch 兜底、去重（seen_jobs.json + tracker）、快速匹配（high/medium/low + Language Gate 覆盖）、大规模发布检测、**门户健康检查**（退化扫描/哨兵探测/rate-limit 诚实报告）、高/中匹配生成 LinkedIn 推荐联系人链接 | `.claude/skills/job-scraper/SKILL.md` + `search-queries.md` | `tests/test_scrape_contract.py`（Step2 契约字段跨门户一致性）；`tests/test_scrape_provenance.py`（source 字段 cli/websearch） |
| B04 | /rank 批量评分排序 | 并行 agent 抓取并五维评分、按 04 权重聚合（Technical 30/Experience 25/Behavioral 15/Career 30）、location/language veto（FAIL 排除、FLAG 带 ⚠）、deadline 紧急度（7 天 🔥）与过期清扫（防御性日期解析、不猜值、--all 可复活）、持久化 rank_score/verdict/location_verdict/language_gate/strengths/gaps | `.claude/commands/rank.md` | `tests/test_rank_command.py`（23 用例：veto 字段持久化/清扫边界/null deadline 规则） |
| B05 | /interview 阶段化面试准备 | 从应用归档（job_posting.md + 已提交的 cv/求职信 + outcome.md 各阶段反馈）构建阶段适配准备包：可能问题（反馈>缺口>要求>阶段优先）、STAR 映射（含新 STAR 起草）、一致性简报、定制难题、反问、物流；可选 mock interview（07 roleplay 协议）；新事实写回 01-candidate-profile.md | `.claude/commands/interview.md` | 无独立测试文件（文本契约），由 `tests/test_lint_skills.py` 校验结构 |
| B06 | /outcome 结果记录 + 跟进信 | 记录申请结果（progress/resolution 分类）、归档已提交材料（copy 不 move、job_posting.md 不重构）、写 outcome.md（stage 复选框 + 备注）、更新 tracker（**Tracker status vocabulary 规范拼写 + 空间拼写兼容读**、preserve 未解析字段）、跟进信分支（10 天默认阈值、最多 2 次、只用已提交材料中的主张、drafted 行永不跟进）、面试后致谢信触发、3+ 解决后提示 /setup 校准 | `.claude/commands/outcome.md` | `tests/test_outcome_followup.py`（7 用例）；`tests/test_tracker_status_vocab.py` |
| B07 | /expand 能力扩充 | 扫描 documents/（cv/linkedin/diplomas/references）+ GitHub 全仓库 + 其他公开 URL（Kaggle/Scholar 等）+ 课程/证书大纲 web 查证 → 能力地图（5 类）→ 用户确认后只增不改、来源可追溯、行为信号标注推断 | `.claude/commands/expand.md` | 无独立测试（文本契约）；`tests/test_lint_skills.py` |
| B08 | /upskill 技能缺口分析 | 聚合模式（tracker + seen_jobs ranked≥45 合并去重、记录 gaps 优先于推断、空白 fit 不当作 0）或单职位模式（URL）；两轮分析（硬技能 diff + LLM 综合 domain/soft/tooling/credential）、优先级热图、学习计划（联网检索真实资源、时间估算、个性化学习方向）、建议学习顺序、报告落盘 + 前后报告 diff | `.claude/skills/upskill/SKILL.md` | `tests/test_upskill_skill.py`（空白 fit 权重/列清单） |
| B09 | /add-template 自定义模板注册 | 注册 LaTeX/Typst/任意编译为 PDF 的工具链模板：访谈捕获（源扩展名/编译命令/字体/风格规则/页数硬限制/已知坑）、`[PLACEHOLDER]` 脱敏存储、**强制测试编译**（填假数据编译+读 PDF 检查布局+清理产物）、ACTIVE-TEMPLATE 管理块激活（/apply 自动读取）；--list/--use/--use default | `.claude/commands/add-template.md` + `templates/README.md` | `tests/test_lint_skills.py`（命令结构） |
| B10 | /add-portal 门户技能生成器 | 调研门户（搜索 URL 模式/结果结构/robots.txt/ToS/凭证需求）→ 按门户技能契约脚手架（search/detail 命令、--format json\|table\|plain、诚实 UA、退避重试、零运行时依赖、凭证仅环境变量）→ **强制真实查询测试** → 注册（/scrape 自动发现 + CI 自动覆盖）；auth 墙拒绝、受限门户加 personal-use-only 警告 | `.claude/commands/add-portal.md` | 契约被 `tests/test_scrape_contract.py` 与各 CLI 测试强制 |
| B11 | /gmail-sync Gmail 状态同步 | 通过 Gmail 连接器扫描开放申请的状态信号（面试邀请/测评链接/offer/拒绝），查询含 `-in:sent -in:drafts`（保留归档邮件）、公司名 OR 组 + ATS 域 OR 组、分类只用全文不用摘要；**每次写入前批量提案 + 引用来源邮件 + 用户批准**；不猜 hired/offer_declined；冲突未匹配标记转手动 | `.claude/commands/gmail-sync.md` | `tests/test_gmail_sync_command.py`（查询排除 sent/drafts、不限制 inbox） |
| B12 | /html-report 离线仪表盘 | 从 tracker（14 列）+ applications/outcome.md 生成自包含 HTML：统计卡、状态环形图/行业/渠道/漏斗图（**漏斗按历史阶段而非当前状态**、拒绝率排除 offer_declined/withdrawn）、可筛选应用表（转义防注入）、内联 SVG 零外部依赖、完全离线 | `.claude/commands/html-report.md` | `tests/test_html_report_command.py`（9 用例：14 列解析、漏斗历史、拒绝率） |
| B13 | /notion-sync Notion 只读流水线 | 经官方 Notion MCP（OAuth 无 key）发布单向视图：ranked 高分职位 + 全部跟踪申请、Key 幂等 upsert、**页面正文只写一次**（--rebuild 例外）、tracker 状态/deadline 优先、文件只同步文件名（文档永不出机）、静默可选（未连接一行说明干净退出）、契约工具无关可移植 | `.claude/commands/notion-sync.md` | `tests/test_notion_sync_command.py`（5 用例：gitignore 状态文件、文档不同步） |
| B14 | /reset 数据重置 | profile（清技能文件保留框架规则）/ documents（删 documents/ 文件夹）/ all；执行前精确展示将删除内容、需输入 RESET 确认、documents/postings/ 已纳入清除范围 | `.claude/commands/reset.md` | `tests/test_reset_command.py`（预览/删除块覆盖 git 树全部子文件夹） |

### C. 技能方法论知识库（`.claude/skills/job-application-assistant/` 01-09 + 根 CLAUDE.md）

| 编号 | 能力名称 | 能力描述 | 模块/文件位置 | 验证方式（测试/命令） |
|---|---|---|---|---|
| C01 | 候选人画像（01） | 结构化画像：身份（含 **Languages 语言表**，驱动语言门）、教育、专业经历、技能、证书、发表、奖项、参考人；为 /setup/apply/expand/interview 提供单一事实源 | `.claude/skills/job-application-assistant/01-candidate-profile.md` | 占位符完整性由 `tests/test_placeholder_integrity.py`（YOUR_EMAIL）强制 |
| C02 | 行为画像（02） | PI/DISC/性格评估、优势/成长区/理想环境、管理风格偏好；用于求职信语气校准（reviewer 与 interview 均引用） | `02-behavioral-profile.md` | `tests/test_lint_skills.py` |
| C03 | 写作风格（03） | 语气/结构/禁忌（无 em-dash、无陈词滥调、无道歉式弱化）、从历史求职信提取模式 | `03-writing-style.md` | `tests/test_lint_skills.py` |
| C04 | **职位评估框架（04）** | Eligibility Gate（公民/PR/安全许可硬门）+ **Language Gate（未声明 FAIL/级别低 FLAG，/rank 持久化 language_gate/language_note）** + 五维评分（技术 30/经验 25/行为 15/职业 30，地点定性）+ 权重/阈值（Strong 75+）/输出格式/公司研究清单/预申请电话最佳实践；**按职能而非职位头衔匹配** | `04-job-evaluation.md`（framework_version 1.2.4） | `tests/test_rank_command.py::test_evaluation_framework_acknowledges_language_gate_tracking` |
| C05 | **CV 模板与裁剪指南（05）** | moderncv banking 结构、LaTeX 特殊字符转义（% &）、**Relevance-weighted cutting（相关性+独特性+叙事负载三因子裁剪）**、2 页硬预算（\needspace 防孤儿、\enlargethispage 救援）、ATS 可解析性（日期 ASCII 区间） | `05-cv-templates.md` | `tests/test_latex_guidance.py`（转义文档、UTF-8 固定） |
| C06 | 求职信模板（06） | cover.cls 结构、开篇/正文/结尾模板、逐 posting 语言匹配 | `06-cover-letter-templates.md` | `tests/test_latex_guidance.py` |
| C07 | 面试准备框架（07） | STAR 示例（Use for 标签）、高频难题（含"你没有 X"桥接模式）、反问分类、电话/视频提示、roleplay 协议（暖场→技术→行为→难题→反馈） | `07-interview-prep.md` | `tests/test_lint_skills.py` |
| C08 | 申请表单字段（08） | 门户自由文本字段（自我介绍/结构化项目/字符限制 pitch/动机题）起草规则与输出格式 | `08-application-forms.md` | `tests/test_lint_skills.py` |
| C09 | 网络研究信任边界（09） | WebFetch 403 处理（curl 浏览器头重试，经 robots.txt 门）、四步升级顺序、搜索片段是线索非来源、公司主张独立验证、可信边界（posting 不可信） | `09-web-research.md` | `tests/test_robots_check.py`（15+ 用例：门控 fail-closed/软 200/百分比解码） |
| C10 | 全局验证清单（根 CLAUDE.md） | 事实准确性/定向/一致性/质量/PDF 编译验证（MANDATORY）/ATS 文本层验证 6 大节逐项 pass/fail 清单；repo 结构说明 | `CLAUDE.md` | 由 /apply Step6 强制运行 |

### D. 门户搜索 CLI（`.agents/skills/` 6 个，Bun/TypeScript）

| 编号 | 能力名称 | 能力描述 | 模块/文件位置 | 验证方式（测试/命令） |
|---|---|---|---|---|
| D01 | 统一门户 CLI 契约 | 每个门户同构：`search`/`detail` 命令、`--format json|table|plain`、JSON 输出 `{meta, results:[{id,title,company,location,date,url}]}`（缺失 null 不省略）、错误写 stderr JSON + exit 1、诚实 UA（`<portal>-cli/1.0`）、指数退避重试（429/5xx）、404 返回 null、未知 flag exit 1（UNKNOWN_FLAG）、零运行时依赖 | 契约定义于 `.agents/skills/linkedin-search/SKILL.md` + `tests/test_scrape_contract.py` | `tests/test_scrape_contract.py`（跨门户契约字段）；各 CLI `cli-flag-validation.test.ts` |
| D02 | jobindex-search（丹麦） | Jobindex.dk 搜索/详情（Stash 解析、hitcount 总数、apply_deadline_asap→null、丹麦长日期→ISO、detail 重写兼容两种真实布局） | `.agents/skills/jobindex-search/`（cli/src/{cli,helpers}.ts + commands/{search,detail}.ts） | `cli/tests/search-page.test.ts`（5）、`detail-parsing.test.ts`（9）、`parsing.test.ts`（16） |
| D03 | jobnet-search（丹麦政府） | Jobnet.dk API（occupations/suggestions 附加命令、1900-01-01 未披露 deadline 哨兵→null、radius/邮编过滤） | `.agents/skills/jobnet-search/` | `cli/tests/search-normalization.test.ts`（5）、`detail-formatting.test.ts`（4） |
| D04 | jobbank-search（Akademikernes） | 学术求职门户 RSS/JSON-LD 解析（@graph、重复过滤器、滚动 deadline→null、日期→YYYY-MM-DD） | `.agents/skills/jobbank-search/` | `cli/tests/rss-parsing.test.ts`（8）、`detail-jsonld.test.ts`（4） |
| D05 | jobdanmark-search | Jobdanmark.dk API（autocomplete/categories/locations 附加命令、JSON-LD + HTML 兜底双分支、邮编后城市提取、省略展示专用键） | `.agents/skills/jobdanmark-search/` | `cli/tests/search-normalization.test.ts`（8）、`detail-parsing.test.ts`（3）、`detail-jsonld.test.ts`（3） |
| D06 | linkedin-search（国家无关） | LinkedIn 公共 jobs-guest 端点、字段无关、显式 -l 地点 flag（任何市场可用）、--jobage/--jobage-minutes 新鲜度窗口、f_TPR 秒级窗口、零运行时依赖、personal-use-only | `.agents/skills/linkedin-search/` | `cli/tests/parsing.test.ts`（22）、`search.test.ts`（2） |
| D07 | freehire-search（多市场 REST） | freehire.me 聚合器公共 REST API（JSON 无 key）、facet flags（--region/--country/--remote）、--no-description 省 token、结构化结果（skills/seniority/category）、FREEHIRE_API_URL 可指向自托管 | `.agents/skills/freehire-search/` | `cli/tests/commands.test.ts`（12）、`parsing.test.ts`（13） |

### E. 工程工具链（`tools/` + 根 `salary_lookup.py`）

| 编号 | 能力名称 | 能力描述 | 模块/文件位置 | 验证方式（测试/命令） |
|---|---|---|---|---|
| E01 | **薪资基准查询（salary_lookup.py）** | 公司薪资模糊匹配：丹麦/北欧字符 anglicize、法律后缀剥离（A/S、ApS…）、核心词重叠打分（0-100）、城市过滤、--json/--list-all/--validate；索引 vs 基准百分比显示；数据自备（BYO） | `salary_lookup.py` + `tools/README_SALARY_TOOL.md` | `tests/test_salary_lookup.py`（56 用例）；`python salary_lookup.py "Company" --json` |
| E02 | Excel 薪资数据转换 | Excel → salary_data.json：自动识别 Company/Firma 与 City/By 列、count/index 列自动配对、**区域设置安全数字解析（最后出现的分隔符为小数分隔符、整千陷阱拒绝、丹麦复合表头子串匹配）** | `tools/convert_salary_excel.py` | `tests/test_convert_salary_excel.py`（30 用例） |
| E03 | 供应链安全守卫 | .claude/settings.json 权限白名单 + **hooks 白名单（Shai-Hulud 蠕虫向量防护，fail-closed）**、.gitignore 个人数据规则必须存在 + 否定规则白名单、.agents package.json 禁生命周期脚本/trustedDependencies | `tools/security_guards.py` | `tests/test_security_guards.py`（28 用例）；`python tools/security_guards.py` |
| E04 | 技能/命令/设置 lint | SKILL.md frontmatter（name/description）校验、allowed-tools 指向存在文件、命令文件 `# /<name>` 标题、settings.json 形状 | `tools/lint_skills.py` | `tests/test_lint_skills.py`（9 用例）；`python tools/lint_skills.py` |
| E05 | robots.txt 访问门 | RFC 9309 实现：最长匹配、同级 Disallow 优先、Claude-User 与 * 检查、403 时浏览器 UA 回读策略但仍严格服从、软 200 HTML 错误页视为不可读、Disallow 百分号解码、curl argv 加固（-- 终结符、--max-redirs 5） | `tools/robots_check.py` | `tests/test_robots_check.py`（28 用例）；`python tools/robots_check.py <url>` |
| E06 | 框架版本戳守卫 | 阻止框架文件未 bump framework_version 即被修改 | `tools/check_framework_version.py` | `tests/test_check_framework_version.py`（4 用例） |
| E07 | 上游更新预览 | 对比 framework_version 标记，列出哪些个性化文件被上游改动 + 每文件 diff 命令；上游缺失文件给 WARNING | `tools/check_upstream_updates.py` | `tests/test_check_upstream_updates.py`（5 用例） |
| E08 | 提交级上游分诊 | 将落后于上游的提交分类为「值得审查」vs「可能跳过」：git patch-id 识别已移植提交、跳过删除文件提交、.github/upstream-wontport.txt 黑名单；只读报告输出 cherry-pick 命令 | `tools/upstream_triage.py` + `.github/workflows/upstream-watch.yml` | `tests/test_upstream_triage.py` |
| E09 | PDF 验证器 | pdfinfo 页数精确断言 + pdftotext 文本层最小字符数 + 必含文本（空白归一化后），清晰错误信息 | `tools/verify_pdf.py` | `tests/test_verify_pdf.py`；CI `latex-smoke` job 使用 |

### F. 文档/渲染资产与 CI

| 编号 | 能力名称 | 能力描述 | 模块/文件位置 | 验证方式（测试/命令） |
|---|---|---|---|---|
| F01 | moderncv 简历模板 | banking 风格 2 页模板，\namefont 钩子兼容 moderncv 2.3.1-2.5.1、pdfpagemode=UseNone、无方括号 \item 陷阱（Achievement 文本层可提取） | `cv/main_example.tex` | CI latex-smoke：lualatex 编译 + `verify_pdf.py --pages 2 --contains Achievement` |
| F02 | cover.cls 求职信类 + 字体 | 自定义 LaTeX 类（fontspec、textpos 日期块、namesection/companyname/lettercontent 命令）+ Lato/Raleway 字体（19 个字体文件） | `cover_letters/cover.cls` + `cover_example.tex` + `cover_letters/OpenFonts/fonts/` | CI latex-smoke：xelatex 编译 + `verify_pdf.py --pages 1`；SETUP.md 冒烟测试 |
| F03 | 自定义模板目录约定 | templates/cv/<name>/、templates/cover_letters/<name>/ 布局（template.<ext> + TEMPLATE.md 清单 + 类/包 + fonts/） | `templates/README.md` | `/add-template` 流程强制 |
| F04 | 职业材料目录约定 | documents/{cv,linkedin,diplomas,references,postings,applications} 布局；**Subfolder naming 规则**（非字母数字下划线全丢弃，防 `Novo Nordisk A/S` 嵌套） | `documents/README.md` | `tests/test_apply_records_application.py::test_documented_rule_yields_a_single_path_component` |
| F05 | 申请跟踪器 CSV | 14 列标准头（date…source,deadline），Tracker status vocabulary（drafted/applied/interview/offer/hired/rejected/no_response/offer_declined/withdrawn + 空间拼写兼容读），Final/Open 定义 | `job_search_tracker.csv`（运行时生成）+ `.gitignore` 忽略 | `tests/test_tracker_status_vocab.py` |
| F06 | CI 流水线 | 6 个 job：lint（lint_skills + 版本守卫）、security-guards、python-tests（unittest 290 用例）、dependency-review（SBOM 探测）、latex-smoke（lualatex/xelatex 编译 + PDF 文本层断言 + 上游占位符断言）、discover-clis → cli-checks 矩阵（自动发现新门户、typecheck + bun test） | `.github/workflows/ci.yml` | 推送/PR 自动运行；`python -m unittest discover -s tests -t . -v` |
| F07 | 每周上游看护 | 每周跑 upstream_triage 并写入滚动 issue（fork 上禁用、只读 GITHUB_TOKEN） | `.github/workflows/upstream-watch.yml` | `tests/test_upstream_triage.py` 覆盖工作流守卫 |

---

## 二、特别确认项（用户点名能力逐一核验）

| 点名能力 | 核验结论 | 具体位置 |
|---|---|---|
| 职位五维度匹配评分 | ✅ 存在（确定性引擎） | `product/src/resume_product/job_evaluation.py`（EligibilityGate + LanguageGate + 五维加权，权重 30/30/15/25）；方法论源 `04-job-evaluation.md` |
| drafter + reviewer 双 Agent 闭环 | ✅ 存在（双形态） | 产品版：`drafter_reviewer.py`（draft→review→迭代≤N 轮）；命令版：`apply.md` Step 3 生成式 reviewer agent（研究公司 + 事实审计 + PartA/PartB 反馈）→ Step 4 修订 |
| 相关性加权裁剪 | ✅ 存在（双形态） | 产品版：`cv_trim.py`（score=0.5 相关性+0.3 独特性+0.2 依赖度）；命令版：`05-cv-templates.md` §Relevance-weighted cutting + `apply.md` Step 5c |
| LaTeX/PDF 渲染校验循环 | ✅ 存在（双形态） | 产品版：`render/latex_compile.py`（compile_tex + visual_check 页数/孤儿标题/字体）；命令版：`apply.md` Step 5（lualatev/xelatex + 视觉检查 + 迭代），CI `latex-smoke`；`tools/verify_pdf.py` |
| ATS 文本层校验 | ✅ 存在（双形态） | 产品版：`ats_check.py`（8 规则评分）；命令版：`apply.md` Step 5d（pdftotext -layout -enc UTF-8、联系方式字面文本、阅读顺序、关键词覆盖表、诚实缺口） |
| 多模板管理（含 3 风格 CSS 模板） | ✅ 存在 | 产品版：`template_registry.py`（注册/切换/导入）+ `render/templates/{classic,modern,minimal}.css` + custom_css 注入；命令版：`/add-template`（任意工具链、强制测试编译、ACTIVE-TEMPLATE 块） |
| 职业全链路辅助 | ✅ 全部存在 | 面试准备：`interview_prep.py` + `07-interview-prep.md` + `/interview`；结果跟踪：`application_tracker.py` + `job_search_tracker.csv` + `/outcome` + `/gmail-sync`；技能缺口：`skill_gap.py` + `/upskill`；**薪资基准：`salary_benchmark.py`（产品引擎）+ 根 `salary_lookup.py`（模糊匹配 CLI）+ `convert_salary_excel.py`** |

---

## 三、基线规模统计

| 指标 | 数量 | 说明 |
|---|---|---|
| 全树文件总数 | **274** | 含字体/文档/测试/资产（不含 .git） |
| Markdown 文档 | 58 | 根文档 6 + 命令 12 + 技能 9 + 门户 SKILL/url-reference 12 + product 文档等 |
| Python 源码文件 | **36** | product/src 27 + 根 tools 8 + salary_lookup.py 1 |
| TypeScript CLI 源码文件 | **29** | 6 门户 × (cli.ts + helpers.ts + commands/*) |
| 命令文件（.claude/commands/） | 12 | apply/setup/rank/outcome/interview/expand/add-template/add-portal/gmail-sync/html-report/notion-sync/reset |
| 技能（SKILL.md） | 9 | job-application-assistant（01-09）+ job-scraper + upskill + 6 门户 = 10 个 SKILL.md 目录 |
| 门户 CLI 套件 | 6 | jobindex/jobnet/jobbank/jobdanmark/linkedin/freehire |
| 工程工具脚本 | 8 | tools/*.py（不含 salary_lookup.py） |
| 根测试文件 | 25（24 测试模块 + __init__） | **290 个测试函数**（`python -m unittest discover -s tests -t . -v`） |
| product 测试文件 | 13 | **109 个测试函数**（`python -m pytest product/tests -q`） |
| CLI 测试文件（.test.ts） | 40 | **217 个测试用例**（`bun test`，每个门户 cli/ 目录） |
| **测试合计** | **78 文件 / 616 用例** | 290 + 109 + 217 |
| Flask API 端点 | 13 | /api/health…export + 前端页面 |
| MCP 工具数 | 20 | tools/schema.py 声明 + executor.py 实现 |
| 版本 | 主基线 CHANGELOG 1.6.0（2026-08-19）；product v1.7.0（2026-08-28，102 测试全绿） | 记录于 `CHANGELOG.md` 与 `product/CHANGELOG.md` |

**入口命令一览：**

| 用途 | 命令 |
|---|---|
| 网页产品启动 | `python -c "from resume_product.api import create_app; create_app().run(port=5123)"` → http://localhost:5123 |
| MCP 插件 | `pip install -e "product[dev]"` 后 `resume-mcp`（stdio） |
| Python API | `from resume_product.core import enrich_experience, generate_resume, list_role_packs` |
| 产品测试 | `python -m pytest product/tests -q`（109 用例） |
| 框架测试 | `python -m unittest discover -s tests -t . -v`（290 用例） |
| 门户 CLI 测试 | 各门户 `cd .agents/skills/<portal>/cli && bun test` |
| Claude Code 工作流 | `/setup` `/scrape` `/apply` `/rank` `/interview` `/outcome` `/expand` `/upskill` `/add-template` `/add-portal` `/gmail-sync` `/html-report` `/notion-sync` `/reset` |
| 薪资查询 | `python salary_lookup.py "Company" --json` / `--city "..."` / `--validate` |
| 薪资 Excel 转换 | `python tools/convert_salary_excel.py path/to/data.xlsx --source "My Data"` |
| 工程守卫 | `python tools/security_guards.py`、`python tools/lint_skills.py`、`python tools/robots_check.py <url>`、`python tools/check_upstream_updates.py`、`python tools/upstream_triage.py --remote upstream`、`python tools/verify_pdf.py <pdf> --pages N` |
| 渲染资产 | `cd cv && lualatex -interaction=nonstopmode main_example.tex`；`cd cover_letters && xelatex -interaction=nonstopmode cover_example.tex` |

---

**拆解说明**：本清单所有能力均通过逐文件源码/文档阅读确认，验证方式列给出的是实际存在的测试文件或可执行命令，未包含任何臆造项。主基线以「Claude Code 命令工作流 + 门户 CLI + 工程工具」为根框架，以 `product/` 为产品化改造核心（五维匹配、相关性裁剪、ATS 校验、drafter-reviewer、多模板渲染、MCP 20 工具、13 端点 API 均已落地并有测试覆盖），两者共享同一套基线方法论（04-job-evaluation、05-cv-templates、07-interview-prep 等）。
